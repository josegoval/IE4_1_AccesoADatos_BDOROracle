
TALUMNOS
	PK	DNI VARCHAR2(10),
		NOMBRE VARCHAR2(50),
		+ DIRECCION TIPO_DIRECCION,		
			DIRECCION VARCHAR2(50),
			POBLACION VARCHAR2(50),
			CODPOSTAL NUMBER(5),
			PROVINCIA VARCHAR(40),
		+ TELEFONOS VARRAY VARCHAR2(15) [2]
		FECHA_NAC DATE,
	+ REF	CURSO REF CURSO,
	+	TNOTAS	TIPO_TABLA_ANIDADA_NOTAS OF TIPO_NOTAS
			TIPO_NOTAS ES OBJETO DE
				PK REF	TIPO_ASIGNATURA,
					NOTA1EV NUMBER(4,2), = null al crearse [no cuenta]
					NOTA2EV NUMBER(4,2), = null al crearse [no cuenta]
					NOTA3EV NUMBER(4,2), = null al crearse [no cuenta]
					NOTAFJUN NUMBER(4,2), = null al crearse [no cuenta]
					NOTASEPT NUMBER(4,2) = null al crearse [no cuenta]

TASIGNATURAS - TIPO_ASIGNATURA
	PK	COD_ASIG NUMBER(4), [módulo]
		+ NOMBRE VARCHAR2(80),
		TIPO VARCHAR(50),	[optativa, obligatoria, libre configuración]

TCURSOS - TIPO_CURSO
	PK	ID_CURSO NUMBER(4),
		DESCRIPCION VARCHAR2(60),
		NIVEL VARCHAR2(3),
		TURNO CHAR


PL/SQL CRUD Tablas - PROCEDIMIENTOS/FUNCIONES

		*** CREATE ***

--	CREATE_ALUMNO (DNI, NOMBRE, [DIRECCION, POBLACION, CODPOSTAL, PROVINCIA],
		[TELEFONO1, TELEFONO2], FECHA_NAC, ID_CURSO]
		RETURNS BOOLEAN (error=false; noError=true)
			DNI = STRING
			NOMBRE = STRING
			DIRECCION = TIPO_DIRECCION(STRING, STRING, NUMBER, STRING)
			TELEFONOS = VARRAY(2) {STRING, STRING}
			FECHA_NAC = DATE
			CURSO = SELECT CURSO FROM TCURSOS WHERE ID_CURSO = ID_CURSO
			TNOTAS = TIPO_TABLA_ANIDADA(...)
	
	-- MATRICULAR_ASIGNATURA (DNI_ALUMNO, COD_ASIG)
		RETURNS BOOLEAN (error=false; noError=true)
			INSERT INTO ALUMNO.TNOTAS NEW TABLA COD_ASIG WHERE ALUMNO.DNI = DNI_ALUMNO

--	CREATE_ASIGNATURA (COD_ASIG, NOMBRE, TIPO)
		RETURNS BOOLEAN (error=false; noError=true)
			COD_ASIG = NUMBER,
			NOMBRE = STRING,
			TIPO = STRING

--	CREATE_CRUSO (ID_CURSO, DESCRIPCION, NIVEL, TURNO)
		RETURNS BOOLEAN (error=false; noError=true)
			ID_CURSO = STRING,
			DESCRIPCION = STRING,
			NIVEL = STRING,
			TURNO = CHAR



		*** UPDATE ***


--	UPDATE_ALUMNO (...ALUMNO, NO TNOTAS)
		RETURNS BOOLEAN (error=false; noError=true)
			UPDATE ALL FIELDS EXCEPT TNOTAS

[1 UPDATE POR ASIGNATURA]
  [(CREATE_ALUMNO && MATRICULAR_ASIGNATURA*) || (UPDATE_ALUMNO && UPDATE_NOTAS*)]

--	UPDATE_NOTAS (DNI_ALUMNO, COD_ASIGN, NOTA1, NOTA2, NOTA3, NOTAJUN, NOTASEP)
		RETURNS BOOLEAN (error=false; noError=true)
			ALUMNO CON DNI_ALUMNO INSERT EN COD_ASIG ...NOTAS
			

-- 	UPDATE_ASIGNATURA ...

-- 	UPDATE_CURSO ...



		*** DELETE ***


-- 	DELETE_ALUMNO ... 

--	DELETE_ASIGNATURA ... (ELIMINA LAS TABLAS DE LOS ALUMNOS QUE TENGAN ESA ASIGNATURA)

--	DELETE_CURSO ... (PON A NULL EL CURSO DE LOS ALUMNOS QUE ESTÉN EN DICHO CURSO)


		*** READ / SELECT ***

--	GET_ALUMNO (DNI_ALUMNO) ...
		RETURNS SELECT -- NULL SI NO EXISTE
			TODOS LOS CAMPOS Y LOS PASA A OBJETO ALUMNO
				... ALUMNO
				CURSO - OBJETO CURSO
				TNOTAS - ARRAY(OBJETO NOTAS [OBJETO ASIGNATURA, NOTA1, ...]

--	GET_ASIGNATURA (COD_ASIG)
		RETURNS SELECT -- NULL SI NO EXISTE
			A OBJETO ASIGNATURA

--	GET_CURSO (ID_CURSO)
		RETURNS SELECT -- NULL SI NO EXISTE
			A OBJETO CURSO





